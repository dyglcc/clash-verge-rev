# 专线代理（Dedicated Line Proxy）功能 — 修改计划

> 本质：对现有"链式代理"的简化封装。固定为「当前选中节点(入口) → 用户录入的专线IP(出口)」的两段链路，
> 一次录入、一键连接/断开、**重启后保持**，且与原链式代理互斥隐藏避免冲突。
> 不改动订阅、原链式代理及其它任何既有功能。

---

## 一、需求拆解（来自用户描述）

| # | 需求 | 说明 |
|---|------|------|
| 1 | 左侧菜单新增「专线代理」入口 | 新增一个 page + nav 项 |
| 2 | 录入 专线出口：IP、端口、协议、用户名、密码 | 这是一个**自定义出口代理节点**（不在订阅里） |
| 3 | 录入后**保存**，只需设置一次 | 持久化到 verge 配置（非仅 localStorage） |
| 4 | 「连接专线」/「断开专线」两个动作 | 连接=建立链路；断开=拆除 |
| 5 | 连接后：当前在代理页选中的节点=**入口**，专线IP=**出口** | 等价于 2 段链 [当前选中, 专线节点] |
| 6 | 断开后：无任何链式链路 | 清除 dialer-proxy 与注入的专线节点 |
| 7 | **重启 Clash Verge 后只要没断开就一直保持**，必须手动断开 | 核心难点：需进入配置生成管线，重启自动重建 |
| 8 | 连接专线时隐藏原"链式代理"入口，避免用户同时配置出错 | 互斥 |
| 9 | 断开专线不影响订阅及其它操作 | 隔离性 |
| 10 | **验证功能**：检测当前出口 IP 是否=录入的专线 IP，弹窗提示成功/失败，并显示出口 IP（等价 `curl ifconfig.me` 的结果） | 见 §3.6 |

---

## 二、现有链式代理机制（已读代码确认）

**前端**
- `src/pages/proxies.tsx`：`isChainMode` 开关，状态存 `localStorage`（`proxy-chain-mode-enabled` / `proxy-chain-exit-node` / `proxy-chain-group` / `proxy-chain-items`）。
- `src/components/proxy/proxy-chain.tsx`：链编辑 UI + 连接/断开逻辑。连接 = `updateProxyChainConfigInRuntime([节点名...])` 然后 `selectNodeForGroup(组, 出口)`。
- `src/providers/chain-proxy-provider.tsx` / `chain-proxy-context.ts`：链模式 context。
- `src/services/cmds.ts`：`updateProxyChainConfigInRuntime`、`getRuntimeProxyChainConfig`。

**后端（Rust, src-tauri/）**
- `src/cmd/runtime.rs`：命令 `update_proxy_chain_config_in_runtime`、`get_runtime_proxy_chain_config`。
- `src/config/runtime.rs::update_proxy_chain_config(Option<Seq<name>>)`：
  先移除所有 `dialer-proxy`，再对链 `[n0,n1,...]` 设置 `n[i].dialer-proxy = n[i-1]`。
- `src/enhance/mod.rs::enhance()`：**每次配置重建（启动、切订阅、改设置）都会跑**，从 profile 生成完整运行配置。链式的 dialer-proxy 注入**不在**这里，是事后运行时打补丁 → 故不持久。

**结论**：原链式注入是运行时内存补丁，重启即失。专线要"重启保持"，必须把注入做进 `enhance()` 管线，并由持久化的 verge 配置驱动。

---

## 三、设计方案

### 3.1 数据模型（持久化到 verge 配置）

在 `src-tauri/src/config/verge.rs` 的 `IVerge` 增加一个可选字段：

```rust
/// 专线代理配置
pub dedicated_line: Option<DedicatedLineConfig>,

pub struct DedicatedLineConfig {
    pub enabled: bool,            // 是否已"连接专线"
    pub name: String,            // 注入的出口节点名，固定如 "专线出口"
    pub proxy_type: String,      // socks5 / http / ss / ...（见待确认项）
    pub server: String,          // IP
    pub port: u16,
    pub username: Option<String>,
    pub password: Option<String>,
    pub entry_node: Option<String>, // 连接时记录的"入口"= 当时组里选中的节点
    pub group: Option<String>,      // 作用的代理组（GLOBAL 或规则模式下的组）
    pub udp: Option<bool>,
    pub skip_cert_verify: Option<bool>,
}
```

> 录入的 IP/端口/协议/账号密码长期保存；`enabled` 控制连/断；`entry_node`/`group` 在点"连接"时快照。

### 3.2 连接 / 断开 的运行时行为（即时生效）

- **连接专线**：
  1. 读取当前作用组（global→`GLOBAL`；规则模式→当前选中组）的当前 `now` 作为入口，写入 `entry_node`。
  2. 置 `enabled=true`，保存 verge 配置。
  3. 触发配置重建（走 §3.3 注入），随后 `selectNodeForGroup(组, "专线出口")`，关闭旧连接。
- **断开专线**：
  1. 置 `enabled=false`，保存。
  2. 触发配置重建（注入被跳过 → 专线节点与 dialer-proxy 都不再存在）。
  3. 把组选回 `entry_node`（或 DIRECT 兜底），关闭旧连接。

### 3.3 配置生成管线注入（保证重启保持）—— 核心

在 `enhance()` 生成配置后、下发 mihomo 前，新增一步 `apply_dedicated_line(config, &verge)`：

当 `dedicated_line.enabled == true` 时：
1. **注入出口节点**：向 `config.proxies` 追加一个代理定义
   `{name:"专线出口", type, server, port, username, password, ...}`（若同名已存在先移除）。
2. **加入代理组**：把 `"专线出口"` 加进目标组的 `proxies` 列表（以及 GLOBAL，保证可被选中）。
3. **设 dialer-proxy**：`专线出口.dialer-proxy = entry_node`（复用与原链式相同的字段语义）。

因为 `enhance()` 在每次启动/切换都会执行，只要 `enabled` 仍为 true，重启后链路自动重建 → 满足需求 7。
组的"选中=专线出口"由 mihomo 自身的 selection 缓存 + 启动时再 `selectNodeForGroup` 兜底保证。

### 3.4 与原链式代理互斥隐藏（需求 8）

- 在 `proxies.tsx`：当 `dedicated_line.enabled` 为真时，**隐藏/禁用**原链式代理切换按钮（`onToggleChainMode` 那个 Button），并在该处显示一行提示"专线代理已开启"。
- 反之，专线连接前若检测到原链式 `isChainMode` 已开启，提示用户先退出链式（避免双写 dialer-proxy）。

### 3.5 隔离性（需求 9）

- 专线只影响 `proxies` / 目标组 / dialer-proxy，**不碰** profile 文件、订阅、规则、DNS 等。
- 断开后注入步骤整体跳过，配置恢复成纯订阅生成结果，订阅与其它操作零影响。

### 3.6 验证功能（需求 10）

**目的**：一键确认"经代理出去的流量确实从专线 IP 出口"，即配置是否生效。

**实现方式（推荐 B，已确认 §八.5）**：后端新增命令 `verify_dedicated_line_ip()`，复用 `media_unlock_checker` 同款 `reqwest::Client`：
1. 读取 `IVerge.verge_mixed_port`（默认 7897）。
2. 构造客户端时**显式走 clash 端口**：`reqwest::Proxy::all(format!("http://127.0.0.1:{port}"))`，超时约 10s。
3. `GET https://ifconfig.me/ip`（纯文本返回 IP；备用 `https://api.ipify.org`），取返回值 trim 作为**出口 IP**。
4. 返回出口 IP 字符串给前端。

> 为什么不用"按平台 shell 执行 `curl ifconfig.me`"：
> reqwest 走 clash 端口**跨平台、无需 curl、无需配 Tauri shell 权限**，且不依赖是否开了 TUN/系统代理——
> 直接验证"链路本身"对不对，更准。宿主机 `curl ifconfig.me` 只反映"整机当前是否在走代理"，是另一回事（见 §九 备选 A）。

**前端**（专线代理页加一个「验证专线」按钮）：
- 调 `verifyDedicatedLineIp()` 拿到 `egressIp`。
- 比对 `egressIp === config.server`：
  - 相等 → MUI Dialog：`恭喜，专线 IP 配置成功！当前出口 IP：{egressIp}`
  - 不等 → Dialog：`当前出口 IP 为 {egressIp}，还不是专线 IP（{config.server}）`
- 无论成功失败都展示拿到的出口 IP（即用户要的"`curl ifconfig.me` 的结果"）。

**注意（见 §七 风险）**：出口 IP（ifconfig.me 看到的）与录入的专线服务器 IP，对单 IP 的 VPS 通常一致；
若专线服务器**入站 IP ≠ 出站 IP**，严格相等会误报"失败"。失败文案因此只说"不一致"，不武断说"配置错误"。

---

## 四、改动文件清单

### 前端（新增为主，尽量不动既有逻辑）
| 文件 | 改动 |
|------|------|
| `src/pages/dedicated-line.tsx` | **新增**：专线代理页面（录入表单 + 连接/断开按钮 + **验证专线按钮** + 状态显示 + 结果 Dialog） |
| `src/pages/_routers.tsx` | 在 `navItems` 增加一项「专线代理」+ 图标 + 路由 |
| `src/services/cmds.ts` | **新增** 4 个命令封装：`getDedicatedLine` / `saveDedicatedLine` / `setDedicatedLineEnabled` / `verifyDedicatedLineIp` |
| `src/pages/proxies.tsx` | 当专线 enabled 时隐藏原链式按钮（最小改动，加条件渲染） |
| `src/locales/zh/*.json`（及其它语言可后补） | 新增「专线代理」相关文案 key |
| `src/types/...` | 新增 `DedicatedLineConfig` 类型 |

### 后端（Rust）
| 文件 | 改动 |
|------|------|
| `src-tauri/src/config/verge.rs` | `IVerge` 加 `dedicated_line` 字段 + 结构体定义 |
| `src-tauri/src/enhance/mod.rs` | `enhance()` 末尾调用新增的 `apply_dedicated_line()` |
| `src-tauri/src/enhance/dedicated_line.rs` | **新增**：注入出口节点 + 入组 + dialer-proxy 逻辑 |
| `src-tauri/src/cmd/`（如 `verge.rs` 或新建 `dedicated_line.rs`） | 新增命令：读/存配置、设置 enabled 并触发重建、**`verify_dedicated_line_ip`（reqwest 走 mixed-port 查出口 IP）** |
| `src-tauri/src/lib.rs` | 注册新命令到 `invoke_handler` |

> ⚠️ 现实提示：需求 7（重启保持）+ 自定义出口注入**无法纯前端干净实现**，必须改 Rust 后端。
> 若坚持只改前端，替代方案见 §六，但更脆弱。

---

## 五、分阶段实施步骤（每步可验证）

1. **后端数据模型** → 加 `IVerge.dedicated_line` + 结构体。
   验证：`cargo check` 通过；写默认值能正常读写 verge 配置文件。
2. **后端注入逻辑** → `enhance/dedicated_line.rs` + 接入 `enhance()`。
   验证：手动在配置里置 enabled=true，重建后 `get_runtime_yaml` 能看到「专线出口」节点且其 `dialer-proxy` 指向入口。
3. **后端命令 + 注册** → 读/存/启停命令。
   验证：用 `tauri dev` 控制台 invoke 三个命令均成功。
4. **前端页面 + 路由 + 菜单** → 新页面与导航。
   验证：左侧出现「专线代理」，表单可录入并保存，刷新后回填。
5. **连接/断开联调** → 串起组选中、入口快照、重建、关连接。
   验证：连接后在代理页看到出口为专线、入口为原选中；断开后恢复。
6. **互斥隐藏 + 隔离回归** → 专线开启时隐藏链式；断开后订阅/规则不受影响。
   验证：专线开启隐藏链式按钮；**重启 app 专线仍生效**；断开后一切如初。
7. **验证功能** → 后端 `verify_dedicated_line_ip`（reqwest 走 mixed-port）+ 前端「验证专线」按钮 + 结果 Dialog。
   验证：连接专线后点验证，弹出出口 IP；与专线 IP 一致弹"成功"，不一致弹"还不是专线 IP"。
8. **多语言文案补齐 + lint/typecheck**。
   验证：`pnpm typecheck`、`pnpm lint` 通过。

---

## 六、若坚持"纯前端"的替代方案（不推荐）

利用现有 profile「Merge（扩展配置）」机制：由专线页面在 merge 增强里写入「专线出口」proxy + 组成员 + dialer-proxy。
因为 merge 走 `enhance()` 故能持久。但：在前端拼改 merge YAML 易碎、与用户手动编辑的 merge 冲突、回滚复杂。**仅在不愿动 Rust 时考虑。**

**验证功能的备选 A（宿主机 shell curl）**：用 `@tauri-apps/plugin-shell` 按平台执行 `curl ifconfig.me`
（Win/Mac/Linux 的 curl 命令相同，但 Tauri 2 需在 capabilities 里给 shell 插件单独放行 `curl`）。
反映的是"**整机当前是否在走代理**"（依赖 TUN/系统代理已开），与 §3.6 的"验证链路本身"语义不同，且依赖 curl 存在。**不推荐作为主实现**，可作为"系统级出口"补充。

---

## 七、风险与注意

- **协议字段映射**：录入的"协议"需映射成合法 mihomo proxy 字段（socks5/http 用 username/password；ss 需 cipher；vmess/trojan 需更多字段）。先支持哪些协议见待确认项。
- **节点重名**：固定出口名「专线出口」若与订阅节点重名需处理（加前缀/校验）。
- **组选择持久化**：依赖 mihomo selection 缓存，启动时需再兜底 `selectNodeForGroup`。
- **GPL-3.0**：分发需开源你的改动。

---

## 八、已确认的关键决策（2026-06-02）

1. **改 Rust 后端：接受。** 按 §三/§四 的后端方案做（`enhance()` 管线注入 + verge 持久化字段），不走 §六 纯前端替代方案。
2. **出口协议：先支持 `socks5` + `http`。** 两者都用 server/port/username/password，最贴合"IP+端口+账号密码"形态。ss/vmess/trojan 后续再加。
   → §3.1 的 `proxy_type` 表单先只放这两项；§七的协议映射只需处理这两种。
3. **隐藏链式时机：仅在专线已连接（enabled=true）时隐藏链式切换按钮，断开后恢复。**
   → 实现就是 `proxies.tsx` 里那个 `onToggleChainMode` Button 加一个 `!dedicatedLineEnabled` 条件渲染，1 行级改动，不动其它链式逻辑。
4. **作用范围：仅当前组。** global 模式 = `GLOBAL`；规则模式 = 当前选中的组。不做"指定具体组"的额外 UI。
